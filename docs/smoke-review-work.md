# Work smoke review

The current review covers Work and all 20 projects linked from its gallery: **798 full-page captures**, 38 screen-size and actual browser-zoom variations per page. The run includes the latest local source changes on top of `deb378d`, still at app version 0.9.2; the manifest records the exact built-source fingerprint.

Open `reports/smoke-review-work/index.html`. This is a single-image review with page search, size groups, keyboard navigation, scroll-position preservation, approval and flagging, and per-capture notes with export/import. There is no baseline or before/after mode.

All captures completed with zero automated warnings. Screenshot dimensions, zoom, and dashboard interactions passed validation. Human visual review remains pending. The old `reports/smoke-review-0.9.2` review and its baseline images were removed as requested.

See [visual-review.md](visual-review.md) for controls and rerun instructions. Generated screenshots remain local.
