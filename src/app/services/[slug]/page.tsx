import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { services } from "../../content";
export function generateStaticParams(){return services.map(({slug})=>({slug}));}
export default async function ServiceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=services.find(item=>item.slug===slug);if(!service)notFound();return <main className="article-page"><header className="article-hero shell"><div><Link className="back-link" href="/services">← All services</Link><p className="kicker">Service guide · {service.price}</p><h1>{service.name}</h1><p className="intro">{service.intro}</p></div><div className="article-image"><Image src={service.image} alt="Whimsy campaign work" fill priority sizes="(max-width: 700px) 100vw, 45vw"/></div></header><section className="article-body shell"><article><p className="kicker">What we do</p><h2>Useful work, shaped around your real goal.</h2><p>We bring strategy, clear communication, and hands-on execution together. The scope stays understandable, the recommendations stay practical, and every choice connects back to the people you need to reach.</p><p>Whimsy works especially well with local businesses and community organizations that want an experienced partner without adding a large agency process.</p></article><aside className="article-callout"><p className="kicker">What we need</p><ul>{service.need.map(item=><li key={item}>{item}</li>)}</ul></aside><div className="process-block"><p className="kicker">How we do it</p><ol>{service.process.map((item,index)=><li key={item}><b>0{index+1}</b><span>{item}</span></li>)}</ol></div><article><p className="kicker">Deliverables</p><h2>You leave with momentum.</h2><ul className="deliverables">{service.deliverables.map(item=><li key={item}>{item}</li>)}</ul></article></section><section className="article-cta"><div className="shell"><p className="kicker">Ready when you are</p><h2>Let’s make the next step clear.</h2><Link className="button" href="/contact">Book a consultation →</Link></div></section></main>}
